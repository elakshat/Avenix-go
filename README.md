# HerSafeRide

Android/Kotlin ride safety app.

## Local setup

This repository does not commit local API credentials. To build the app, keep these files on your machine:

- `local.properties` with `MAPS_API_KEY=your_google_maps_key`
- `app/google-services.json` from your Firebase project

Then open the project in Android Studio or run the Gradle wrapper.
